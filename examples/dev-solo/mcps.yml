# MCP tool servers available to agents.
# The "devkit" special name in agents.yml provides an in-process llmdevkit mcp
# instance, so no explicit entry is needed here for file tools.
# Add upstream MCP servers below if you have them.

mcps: {}
# Example: uncomment to add an upstream MCP server
#  websearch:
#    url: http://localhost:9001/mcp
#    headers:
#      Authorization: Bearer token123
